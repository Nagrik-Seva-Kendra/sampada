# deps
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# prisma
apps/api/prisma/*.db

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
Thumbs.db

# legacy DB dump (large — keep out of git)
infra/legacy-db/*.sql

# uploaded guideline PDFs (runtime data)
apps/api/uploads/

# production deploy artifacts (bundled, regenerated by `pnpm --filter @sampada/api bundle`)
apps/api/deploy/
deploy-dist/
nsk-deploy.zip
